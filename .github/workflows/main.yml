- name: Run Local Docker Commands
      run: |
        echo "Starte einen Nginx-Container..."
        docker run -d -p 80:80 --name mein_webserver nginx
        
        echo "Aktuelle Container:"
        docker ps
